import { useState, type FormEvent } from "react";

function Dumper() {
    const startDump = () => {
        fetch()
    }
    return (
        <>
            <p>
                x
            </p>
        </>
    );
}

export default Dumper;
